using UnityEngine;

public class ItemScript : MonoBehaviour
{
    public float itemValue = 0f; // Value of the item, can be set in the Inspector

    void Start()
    {
        // Optional: Instead of using a random value, you can assign itemValue directly in the Inspector.
        // Remove the random assignment or customize it as needed.
    }
}